"""Callbacks for review queue page.

Add callback registrations here via the register_callbacks(app) entry point.
"""

from dash import Dash


def register_callbacks(app: Dash) -> None:  # noqa: D401
    """Register review-related callbacks (placeholder)."""
    # To be populated as part of full extraction
    return None
